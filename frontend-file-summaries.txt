{
  "frontend/src/App.tsx": "Root app component that renders the ChatView as the sole page.",
  "frontend/src/hooks/useQueries.ts": "Complete rewrite of all React Query hooks; fixes useGetCallerUserProfile (proper pattern with actorFetching, retry:false, isFetched), useCreateUserProfile (invalidates 'currentUserProfile'), usePostMessage (invalidates 'messages'), usePendingFriendRequests (calls getPendingFriendRequests, returns FriendRequest[]), useSendFriendRequest/useAcceptFriendRequest/useDeclineFriendRequest (correct query key invalidation), useFriendsProfiles (fetches profiles for all friends), usePrivateThread/useSendPrivateMessage (correct Principal-based thread queries), and adds missing useDeclineFriendRequest hook.",
  "frontend/src/components/ChatView.tsx": "Main chat view that now passes the new `remoteAudioRef` prop from `useVideoCall` down to `VideoCallModal`, alongside all existing call state and handlers.",
  "frontend/src/components/MessageFeed.tsx": "Scrollable public chat feed with auto-scroll via bottomRef div, chronological message sorting, own/other message distinction, avatar initials, and proper overflow container with minHeight:0.",
  "frontend/src/components/MessageInput.tsx": "Controlled input form that calls usePostMessage mutation, clears input only on success, shows spinner during pending state, and prevents duplicate submissions.",
  "frontend/src/components/DisplayNamePrompt.tsx": "Simplified onboarding prompt that creates a profile via useCreateUserProfile, auto-generating a username from the display name, and calls onComplete after success.",
  "frontend/src/index.css": "Global OKLCH color tokens for light/dark modes plus new video call semantic tokens (call-bg, call-remote, call-controls, call-pip, call-text).",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter font, coral/amber/teal color tokens, border-radius scale, shadows, and new call-* semantic color mappings for the video call UI.",
  "frontend/src/components/ProfileSetupModal.tsx": "Full-screen profile creation modal calling useCreateUserProfile mutation with username and displayName, invalidates 'currentUserProfile' on success, and shows error messages for duplicate usernames.",
  "frontend/src/components/FriendListItem.tsx": "Friend list item with avatar, online status dot (checks isOnline flag and lastOnline within 5 minutes), and optional video call button that stops click propagation.",
  "frontend/src/components/FriendRequestItem.tsx": "Friend request item accepting both a FriendRequest and UserProfile prop; calls useAcceptFriendRequest and useDeclineFriendRequest mutations with correct fromUser Principal and inline loading spinners.",
  "frontend/src/components/FriendsPanel.tsx": "Friends sidebar with search (useSearchUsers), pending requests (usePendingFriendRequests returning FriendRequest[]), and friends list (useFriendsProfiles); uses PendingRequestRow sub-component to look up each requester's profile before rendering FriendRequestItem.",
  "frontend/src/components/PrivateChatView.tsx": "Private one-on-one chat view; uses threadStartedRef to prevent repeated startPrivateThread calls, auto-scrolls via bottomRef, sends messages via useSendPrivateMessage with friendPrincipal as threadId, and compares msg.sender to currentUserProfile.principal.toString() for own-message detection.",
  "frontend/index.html": "HTML entry point with updated title \"Friends Chat – Stay connected with friends\", meta description, Open Graph tags (og:title, og:description, og:image), Twitter Card tags, and favicon/apple-touch-icon all pointing to the generated app icon for social sharing and App Market publishing.",
  "frontend/src/hooks/useVideoCall.ts": "Video call hook that manages local camera/mic stream and a simulated remote audio stream; exposes `remoteAudioRef` (new) alongside `localVideoRef`, creates a cloned audio track as the remote stream on `startCall`, and properly stops and clears both streams and their element srcObjects on `endCall` and unmount.",
  "frontend/src/components/VideoCallModal.tsx": "Video call overlay that now includes a hidden `<audio>` element (not muted, autoPlay) wired to `remoteAudioRef` for remote audio playback; uses callback refs to sync both the local video and remote audio elements with the hook's refs, and plays audio as soon as srcObject is available."
}