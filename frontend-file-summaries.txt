{
  "frontend/src/App.tsx": "Root app component that renders the ChatView as the sole page.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds back `useFriendsProfiles` (fetches full UserProfile objects for the friends list) and `usePendingFriendRequests` as an alias for `useGetPendingFriendRequests` to restore compatibility with FriendsPanel.",
  "frontend/src/components/ChatView.tsx": "Main chat layout with auth-gating; fixes the `loginStatus === 'error'` type comparison via a string cast, passes the required `currentUserProfile` prop to `MessageFeed` and `MessageInput`, and passes all required props (`currentUserProfile`, `onSelectFriend`, `selectedFriend`, `onStartVideoCall`) to `FriendsPanel`.",
  "frontend/src/components/MessageFeed.tsx": "Scrollable public chat feed with color-coded avatars, own/other message distinction, timestamps, and auto-scroll to bottom.",
  "frontend/src/components/MessageInput.tsx": "Controlled input form for posting public messages with send button, Enter-to-send, and loading spinner.",
  "frontend/src/components/DisplayNamePrompt.tsx": "Compact onboarding card for entering a display name; guards submission against an uninitialised actor, shows a \"Connecting…\" spinner banner, and surfaces clear error messages.",
  "frontend/src/index.css": "Global OKLCH color tokens for light/dark modes plus new video call semantic tokens (call-bg, call-remote, call-controls, call-pip, call-text).",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter font, coral/amber/teal color tokens, border-radius scale, shadows, and new call-* semantic color mappings for the video call UI.",
  "frontend/src/components/ProfileSetupModal.tsx": "Full-screen profile creation page with username and display name fields, actor-readiness guard (shows \"Connecting…\" banner and disables submit), and user-friendly error mapping for network/actor errors.",
  "frontend/src/components/FriendListItem.tsx": "Friend list row with avatar, online status dot, display name, and a video call button that appears on hover.",
  "frontend/src/components/FriendRequestItem.tsx": "Friend request row with requester avatar, name, and accept/decline buttons with inline loading spinners.",
  "frontend/src/components/FriendsPanel.tsx": "Friends sidebar with search, Friends/Requests tabs, friend list, and pending requests; updated imports to use `useGetPendingFriendRequests` and `useFriendsProfiles` (the correct exported names from useQueries).",
  "frontend/src/components/PrivateChatView.tsx": "Private one-on-one chat view with header (back button + video call button), scrollable message thread, and send form; fixed import to use `useGetPrivateThread` instead of the old `usePrivateThread` name.",
  "frontend/index.html": "HTML entry point with updated title \"Friends Chat – Stay connected with friends\", meta description, Open Graph tags (og:title, og:description, og:image), Twitter Card tags, and favicon/apple-touch-icon all pointing to the generated app icon for social sharing and App Market publishing.",
  "frontend/src/hooks/useVideoCall.ts": "Video call hook that manages local camera/mic stream and a simulated remote audio stream; exposes `remoteAudioRef` (new) alongside `localVideoRef`, creates a cloned audio track as the remote stream on `startCall`, and properly stops and clears both streams and their element srcObjects on `endCall` and unmount.",
  "frontend/src/components/VideoCallModal.tsx": "Full-screen video call overlay accepting nullable ref types (`RefObject<HTMLVideoElement | null>` and `RefObject<HTMLAudioElement | null>`) to match the `useVideoCall` hook's return types; displays remote avatar, local video PiP, hidden remote audio, and mute/camera/end-call controls."
}